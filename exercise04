# 4. Пользователь вводит целое положительное число.
#  Найдите самую большую цифру в числе. Для решения используйте цикл while и арифметические операции.



user_count = int(input("Введите положительное целое число >>>"))

max_count_into_user_count = user_count % 10

user_count = user_count // 10

while user_count > 0:

    if user_count % 10 > max_count_into_user_count:

        max_count_into_user_count = user_count

    user_count = user_count // 10

print(max_count_into_user_count)
